git config
git config --global user.name "Tony"
git config --global user.email "jayton006@gmail.com"
git config --global --list
git remote -v
git config --list


- finish up on this porfolio, to be included in the list

resend email api intergration with the python site
create ts api

learn some ts, even if it is a crash course, stay sharp